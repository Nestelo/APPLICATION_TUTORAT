# Tutorat INSTA - Frontend

Application mobile de tutorat et partage de ressources académiques.

## Installation

1. Cloner le dépôt
2. `npm install` ou `yarn install`
3. `npx expo start`

## Connexion au backend

Modifier l'URL de l'API dans `src/api/axiosConfig.js` (variable `BASE_URL`).